# App dependencies only — excludes PyTorch / torchvision / PyTorch3D / numpy.
# Python 3.11+ (RunPod: runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04).
#
#   pip install -r requirements.txt

# Conda Python packages (pip equivalents)
certifi>=2022.12.7
charset-normalizer>=2.0.4
colorama>=0.4.6
cryptography>=39.0.1
fvcore>=0.1.5
idna>=3.4
iopath>=0.1.9
pillow>=9.4.0
portalocker>=2.7.0
PyYAML>=6.0
requests>=2.28.1
six>=1.16.0
tabulate>=0.9.0
termcolor>=2.2.0
tqdm>=4.65.0
typing_extensions>=4.4.0
urllib3>=1.26.14
yacs>=0.1.8

# Tracker runtime (direct imports in this repo)
loguru>=0.6.0
mediapipe>=0.10.9
opencv-contrib-python>=4.7.0
face-alignment>=1.3.5
scikit-image>=0.22.0
scipy>=1.11.0
trimesh>=3.20.0
matplotlib>=3.7.0

# Transitive / optional in original env (not imported by metrical-tracker)
absl-py>=1.4.0
attrs>=22.2.0
imageio>=2.26.0
networkx>=3.0
packaging>=23.0
protobuf>=3.20.3,<5
python-dateutil>=2.8.2
tifffile>=2023.3.15

# TensorBoard (tracker.py uses SummaryWriter)
tensorboard>=2.12.0

# Omitted — Python 3.11 incompatible or unused:
#   llvmlite==0.39.1, numba==0.56.4  (py3.11 needs llvmlite>=0.41; not used here)
#   chumpy==0.70                     (not imported; breaks on modern Python)
#   torch, torchvision, pytorch3d, numpy (preinstalled on RunPod image)
